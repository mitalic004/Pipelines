name: Mitali Example Workflow

# When should this run?
on:
  pull_request:
    branch:
      - main

# What should this do?
jobs:
  example_workflow:
    runs-on: ubuntu-latest

    steps:
    - name: Say hello
      run: echo "Hello world!"